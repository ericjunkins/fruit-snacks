import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
    colors: {
        primary: "#35b9d4",
        grape: "#9D2690",
        strawberry: "#e01709",
        orange: "#FF8400",
        peach: "#FF9477",
        raspberry: "#e30b5d"
    }
})

export default theme;